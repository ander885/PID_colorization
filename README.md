# PID Colorization — baseline OpenCV-DNN (caffe)

## Preparación
1. Python 3.10+, `pip install opencv-python numpy`
2. Descargar modelo Caffe (local, no versionado):
   ```powershell
   powershell -ExecutionPolicy Bypass -File scripts\download_models.ps1
